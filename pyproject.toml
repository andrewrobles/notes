[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "notes"
version = "0.1.0"

[tool.setuptools]
packages = ["app", "app.bin"]


[project.scripts]
note = "app.bin.note:main"